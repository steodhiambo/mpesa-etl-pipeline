# Database Configuration
DB_TYPE=postgresql
POSTGRES_DB=mpesa_analytics
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_me_in_production
POSTGRES_PORT=5432

# Airflow Configuration
AIRFLOW_HOME=/opt/airflow
AIRFLOW_PORT=8080
AIRFLOW_ADMIN_USER=admin
AIRFLOW_ADMIN_EMAIL=admin@example.com
AIRFLOW_ADMIN_PASSWORD=change_me_in_production

# Generate these keys using: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
AIRFLOW_FERNET_KEY=change_me_in_production
AIRFLOW_SECRET_KEY=change_me_in_production

# Logging
LOG_LEVEL=INFO

# Security
ENVIRONMENT=development